#include <iostream>
#include <fstream>
using namespace std;

int main()
{
	ofstream off("matrice.txt");
	
	for (int i=0; i<100; ++i)
	{
		for (int j=0; j<100; ++j)
		{
			if(j>=i) off << 1;
			else off << 0;
			off << '\n';
		}
	
	}
}
